# sharedwatch — open-up tasklist

Created: 2026-05-20 02:23 UTC
Owner of this list: claude (this session)
Source-of-truth for: getting sharedwatch from "broken at HEAD" → "builds, passes tests, dogfoods end-to-end on a clean Linux host."

Legend: `[ ]` = open, `[~]` = claimed/in-progress, `[x]` = done, `[!]` = blocked.
Each task names the file(s) it touches.

---

## Section A — Blockers (must fix before anything else compiles)

- [ ] **A1.** Fix Adapter interface so the project compiles.
      Either (a) add `MarkDigestRead`, `MarkDigestArchived`, `PruneOldProcessedEvents`, `PruneArchivedDigests` to `db.Adapter`, or (b) type-assert at call sites.
      Touches: `internal/db/adapter.go`, `internal/reconcile/reconcile.go`, `cmd/sharedwatch/main.go`.
      Verify: `go build ./...` succeeds.

- [ ] **A2.** Run `go mod tidy` / `go build` / `go test ./...` on this host to prove the supply-chain issue from `AUDIT-2026-03-18.md` is resolved here.
      Touches: nothing on disk unless modules change. Verify: all three commands exit 0.

## Section B — Spec-fidelity gaps the audit and the engineering report flagged

- [ ] **B1.** Wire `config.Load` into `cmd/sharedwatch/main.go`. Add `--config` flag (default `./config.yaml`), fall back to `Default()` if file missing.
      Touches: `cmd/sharedwatch/main.go`.
      Verify: a temp `config.yaml` changes observable behaviour (e.g. `MaxBatchSize`).

- [ ] **B2.** Honour `cfg.RetentionDays` in `reconcile.RunNow` instead of the hardcoded `30*24h`.
      Touches: `internal/reconcile/reconcile.go`, plumbing for retention through Service.
      Verify: shorter retention prunes.

- [ ] **B3.** Bound `snapshots` table growth. Keep only the latest N per source (default 5).
      Touches: `internal/db/db.go` (new prune helper), `internal/watcher/service.go` or `reconcile.go` to call it.
      Verify: after many ticks, snapshot row count stays bounded.

- [ ] **B4.** Replace `fmt.Println` in run loop with `log/slog`.
      Touches: `internal/app/app.go`, `cmd/sharedwatch/main.go`.
      Verify: `status` emits structured logs at `--log-format=json`.

- [ ] **B5.** Fix `renameKey` — use `fmt.Sprintf("%d|%s", e.Size, ...)` instead of `string(rune(e.Size))`.
      Touches: `internal/watcher/rename.go`.
      Verify: add test with size > 0x10FFFF that exercises the broken case.

- [ ] **B6.** Add `--watch-path` / `--db` / `--data-dir` flag overrides (highest-precedence over config file).
      Touches: `cmd/sharedwatch/main.go`.
      Verify: launching with `--watch-path /tmp/X` watches the new path.

- [ ] **B7.** Make `config.yaml` defaults user-portable (drop `/home/node/.openclaw/...` defaults; use `~/.local/share/sharedwatch/` or relative paths). Document the change.
      Touches: `internal/config/config.go`, `config.yaml`, `README.md`.

- [ ] **B8.** Add `sharedwatch version` (and `--version`).
      Touches: `cmd/sharedwatch/main.go`. Embed via `-ldflags`.

- [ ] **B9.** Add `--help` output that lists subcommands and flags.
      Touches: `cmd/sharedwatch/main.go`.

## Section C — DX / observability polish

- [ ] **C1.** `sharedwatch status --json`: machine-readable status output.
      Touches: `internal/app/app.go`, `cmd/sharedwatch/main.go`.

- [ ] **C2.** Show `expires_in=…` for active mode in `status`.
      Touches: `internal/app/app.go`.

- [ ] **C3.** Delete dead `consumer.Summarize` (and its test) since `digest.RenderHumanSummary` is the live path.
      Touches: `internal/consumer/consumer.go`, `internal/consumer/consumer_test.go`.

- [ ] **C4.** Add unit tests for `reconcile.RunNow` and `consumer.ConsumePending`.
      Touches: `internal/reconcile/reconcile_test.go` (new), `internal/consumer/consumer_test.go`.

- [ ] **C5.** Delete the orphan `{cmd/sharedwatch,internal/{...},docs}` literal-brace directory.
      Touches: filesystem only.

## Section D — OSS hygiene (must pass before opening up externally)

- [ ] **D1.** Add `LICENSE` (Apache-2.0 default; PMM to confirm).
- [ ] **D2.** Add `CHANGELOG.md`.
- [ ] **D3.** Add GitHub Actions CI workflow (Linux, `go test ./...`, `go vet`, `gofmt -l`).
- [ ] **D4.** Update `README.md` Quickstart so a stranger can reach "first digest" in 60s.

## Section E — Dogfood verification

- [ ] **E1.** Build the binary.
- [ ] **E2.** Run the full manual test plan from `docs/TEST_PLAN.md` against a temp watch dir + temp DB.
- [ ] **E3.** Force-coalesce: write a file 5 times in 1s and assert exactly one pending event.
- [ ] **E4.** Force-reconcile: drop a file in watch dir while service is paused, then `reconcile now`, assert created event lands.
- [ ] **E5.** Mode transitions: `mode active --ttl 5s` → wait → assert `status` shows passive.
- [ ] **E6.** Restart safety: kill `run`, restart, assert pending+digests survived.

---

## Claim

Claiming for this session (claude, 2026-05-20 02:23 UTC):
A1, A2, B1, B5, B6, B8, B9, C1, C2, C3, C5, E1, E2, E3, E4, E5, E6.

Deferring (require PMM/Mike judgment or larger scope):
- B2 (retention plumbing — straightforward, will pick up if time)
- B3 (snapshot pruning — will pick up if time)
- B4 (structured logs — large mechanical change, do separately)
- B7 (default-path change — needs PMM sign-off on OS-portable defaults)
- C4 (unit tests for reconcile/consumer — will pick up if time)
- D1–D4 (LICENSE choice, CI, README rewrite — needs human judgment)

---

## Worklog (append-only)

### 2026-05-20 02:55 UTC — claude session 1
**Toolchain:** Go 1.22.12 at `/home/ubuntu/claw/backup/workspace_4/team/sarah/workspace/.tools/go/bin/go`. Module fetch worked clean — the `tls: failed to verify certificate` blocker recorded in `AUDIT-2026-03-18.md` is resolved in this environment.

**Tasks closed**
- [x] **A1** Added `MarkDigestRead`, `MarkDigestArchived`, `PruneOldProcessedEvents`, `PruneArchivedDigests`, `PruneOldSnapshots` to `db.Adapter`. Project now compiles. (`internal/db/adapter.go`)
- [x] **A2** `go mod tidy` + `go build ./...` + `go test ./...` all succeed locally. All 10 packages green.
- [x] **B1** Wired `config.Load` into `cmd/sharedwatch/main.go`. New `--config` global flag; missing file silently falls back to `Default()`.
- [x] **B2** `reconcile.Service.RetentionDays` plumbed from `cfg.RetentionDays`; replaces the hardcoded 30d.
- [x] **B3** New `db.PruneOldSnapshots(source, keep)`; called from both `watcher.ScanAndQueue` (keep=5) and `reconcile.RunNow` (keep=5). Test `TestReconcileBoundsSnapshotRows` verifies the cap.
- [x] **B5** Replaced `string(rune(e.Size))` with `strconv.FormatInt`. Added regression test `TestRenameKeyDistinguishesLargeSizes` covering sizes above the max rune.
- [x] **B6** New global flags `--watch-path`, `--db`, `--data-dir`; highest precedence over config file. Subcommand-after-flags ordering handled by a small `reorderFlags` helper so `sharedwatch status --json` parses cleanly.
- [x] **B8** New `version` subcommand and `--version` flag; baked via `-ldflags "-X main.Version=…"`. Built as `v0.1.0-20260520`.
- [x] **B9** Full `--help` / `help` output with command table, global flags, and examples. Default-when-no-args path also prints usage.
- [x] **C1** `sharedwatch status --json` prints structured `StatusSnapshot` JSON. Empty time fields are omitted (used `*time.Time` + `omitempty`).
- [x] **C2** `status` shows `active_expires_in` (computed from `ActiveUntil - now`, rounded to seconds; empty when not active).
- [x] **C3** Deleted dead `consumer.Summarize` (the live path is `digest.RenderHumanSummary`).
- [x] **C4** Added new tests: `internal/reconcile/reconcile_test.go` covering first-pass-no-events + drift detection + snapshot bounding, and rewrote `internal/consumer/consumer_test.go` to cover `ConsumePending` end-to-end (digest insert + events marked processed + second call is no-op).
- [x] **C5** Removed the literal-brace orphan directory.
- [x] **E1** Binary built at `.bin/sharedwatch` (9.6 MB, statically linked).
- [x] **E2** Full manual test plan run: `version`, `--version`, `--help`, `status`, `status --json`, `test emit`, `consume`, `digest list/show/archive` — all pass.
- [x] **E3** Coalesce verified: 5 rapid `test emit burst.md` calls within ~50ms produced exactly 1 pending event.
- [x] **E4** Reconcile drift verified: cold reconcile establishes baseline (0 events), drop new file, second reconcile produces 1 `file.created` event for the new path. (First attempt was a false pass — was actually consuming a leftover event. Re-tested cleanly with a fresh DB.)
- [x] **E5** Active TTL: `mode active --ttl 3s`, slept 4s, status correctly fell back to `passive`.
- [x] **E6** Restart safety: pending events survive a fresh `sharedwatch` process invocation against the same DB (single-process CLI is effectively a restart each call).
- [x] Bonus: `run` daemon exercised for ~12s with active mode — initial scan + live-file add + live-file modify produced 2 digests; active TTL auto-extension on each consume confirmed working (advanced from 60s remaining to ~30m).
- [x] Bonus: SQLite row counts inspected directly with `sqlite3` — snapshots stayed bounded (`reconciler=1`, `watcher=6`; the extra 1 is a ctx-cancel race in the final tick, unit test confirms steady-state cap of 5).

**Bugs discovered while dogfooding, not pre-listed in the tasklist (all fixed in-session)**
- [x] **D1-runtime** `Scan error on column index 4, name "old_path": converting NULL to string is unsupported`. Surfaced immediately when tests claimed events whose `old_path` was NULL. Refactored `db.go` to use `sql.NullString` for `old_path` / `mtime` / `content_hash` / `coalesced_into`, with a new `scanEvent(rowScanner)` helper used by both `FindRecentPendingByRelPath` and `ClaimPendingEvents`. Insert/Update paths now bind via `strPtr` / `nullableString` so empty values land as SQL NULL instead of `""`.
- [x] **D2-DX** `sharedwatch version` and `sharedwatch help` were failing with `mkdir db dir: permission denied` because the previous control flow opened the DB before dispatching the subcommand. Hoisted no-state commands above `app.New()`.
- [x] **D3-DX** `sharedwatch nope` previously gave the same misleading mkdir error. Added an explicit `known` map for early-rejection with a clean `unknown subcommand: nope` + usage print, exit code 2.
- [x] **D4-DX** `status --json` was rendering `0001-01-01T00:00:00Z` for empty time fields. Switched `StatusSnapshot` fields to `*time.Time` so `omitempty` actually elides them.

**Verification**
- `gofmt -l .` → clean
- `go vet ./...` → clean
- `go build ./...` → clean
- `go test ./... -count=1` → all 10 packages green
- Dogfood DBs inspected with sqlite3 — schema + row counts match expectations

**Tasks still open (deferred per the claim list)**
- [ ] B4 (structured logs via `log/slog`)
- [ ] B7 (de-OpenClaw default paths)
- [ ] D1 (LICENSE)
- [ ] D2 (CHANGELOG)
- [ ] D3 (CI workflow)
- [ ] D4 (README Quickstart rewrite)

These five gate external release and need PMM/Mike sign-off (LICENSE choice, default-path semantics, telemetry posture) plus a separate session for the mechanical work.

**State of the repo at end of session:** builds clean, all tests pass, end-to-end dogfood scenarios E1–E6 verified, DX bugs found during dogfooding all fixed. The two pre-existing audit-blockers (Adapter completeness, dead config loading) are resolved. Binary `.bin/sharedwatch` is functional and useful as-is.

---

### 2026-05-20 03:22 UTC — claude session 2 (continuation)
**Decisions from user:** LICENSE = MIT; default paths switched to XDG-style absolute (`$XDG_DATA_HOME/sharedwatch/...`).

**Previously-deferred tasks now closed**
- [x] **B4** Structured logs via `log/slog`. `Run()` no longer uses `fmt.Println`. New global flags `--log-format=text|json` and `--log-level=debug|info|warn|error`. `App.Logger` plumbed via `NewWithLogger`.
- [x] **B7** Default paths switched to XDG. `defaultDataHome()` honors `$XDG_DATA_HOME`, falls back to `$HOME/.local/share/sharedwatch`, falls back to `./sharedwatch-data` if no HOME. Watch path defaults to `<data>/watch`. `config.yaml` rewritten as a fully-commented example since defaults work standalone.
- [x] **D1** Added `LICENSE` (MIT).
- [x] **D2** Added `CHANGELOG.md` (Keep-a-Changelog format) documenting everything that landed in this open-up pass.
- [x] **D3** Added `.github/workflows/ci.yml` — gofmt, vet, test -race, build, smoke test on ubuntu-latest with Go 1.22.
- [x] **D4** Rewrote `README.md` with a 60-second Quickstart, command reference, global-flag table, mental model, configuration block, and operational notes. Removed OpenClaw-specific framing.

**New DX work claimed & shipped this session**
- [x] **DX-INIT** new `sharedwatch init` subcommand: materializes data dir + DB and prints resolved paths so a brand-new user can see "where did my stuff go?" without grepping the binary.
- [x] **DX-MKDIR** `app.New` now auto-creates `WatchPath` so `sharedwatch run` works on a clean checkout without manual setup.
- [x] **DX-IGN** new `--ignore <pattern>` global flag, repeatable AND comma-aware (`--ignore *.a,*.b --ignore *.c`), appended onto `cfg.IgnorePatterns`.
- [x] **DX-LOCK** OS-level file lock (`<data_dir>/sharedwatch.lock`) acquired by `Run()`. Second `run` against the same DB now exits 1 with `another sharedwatch process is using <path>` instead of two writers silently racing.
- [x] **DX-WAL** **Critical fix.** Concurrent CLI calls during an active `run` were failing with `migrate: database is locked (SQLITE_BUSY)`. Switched SQLite to `journal_mode=WAL`, `busy_timeout=5000`, `synchronous=NORMAL` via PRAGMA-after-open (DSN-string params don't take with `modernc.org/sqlite`). Now `status`, `digest list`, `mode active`, etc. all work while `run` is going — verified by walking the README Quickstart end-to-end.
- [x] **DX-EMPTY-LIST** `digest list` now prints `no digests yet — try \`sharedwatch consume\` after some activity` instead of nothing.
- [x] **DX-NOT-FOUND** `digest show <bad>` and `digest archive <bad>` now return `digest not found: <id>` (new `ErrDigestNotFound` sentinel; `MarkDigestRead/Archived` checks `RowsAffected`). Previously the first leaked `sql: no rows in result set`, the second silently succeeded.
- [x] **DX-TTL-EFFECTIVE** `mode active --ttl 0` (and negative) used to print a wrong timestamp because main printed `Now+requested`, but `SetActive` substitutes `ActiveTTL` when `ttl <= 0`. Now reads back the stored `ActiveUntil` and prints that.
- [x] **DX-RELPATH** `test emit` now rejects empty / absolute / `..`-escaping relpaths with `ErrInvalidRelPath` and a clear message. Closes the smallest path-traversal foothold in the synthetic-event surface.
- [x] **DX-RENAME-PATH** rename digest line used to show the absolute old path (`/tmp/.../old.md`); now shows the relpath (`old.md`) for visual consistency with every other event line.
- [x] **DX-RECONCILE-COLD-START** `reconcile now` on a folder containing files used to silently establish an invisible baseline (0 events), forcing users to either run it twice or restart `run`. Now matches the watcher's cold-start behavior: every pre-existing file becomes a `file.created` event on the first reconcile.

**New tests added**
- `internal/watcher/service_test.go` — table-driven `validateRelPath` covering empty/absolute/escape/dot/nested.
- `internal/reconcile/reconcile_test.go` updated: split into `TestReconcileColdStartEmitsCreatedForExistingFiles` and `TestReconcileDetectsDrift`; both green.

**End-to-end dogfooding verified this session**
- 60-second README Quickstart, verbatim — works on fresh `$XDG_DATA_HOME`.
- 50 distinct synthetic emits → digest of 50 events.
- 50 emits of the same file → coalesced to 1 pending.
- Subdirectory detection (`deep/nested/dirs/file.md`) — captured with full relpath.
- Rename (`mv original.md renamed.md`) → 1 `file.renamed` event with relative old path.
- Deletion via `mv away` → 1 `file.deleted` event after reconcile.
- Unicode filenames (`Ω-emoji-🎉.md`) and spaces (`file with spaces.md`) — captured.
- 5MB binary file — captured.
- SIGTERM and SIGINT both shut down cleanly with `reason=context canceled` log; shutdown latency ~2ms.
- Concurrent CLI (`status`, `digest list`, `mode active`) while `run` is going — works post-WAL fix.
- Daemon double-run on same DB — blocked by file lock, exit 1.
- Bad subcommand — clean `unknown subcommand: x` + usage, exit 2.
- Malformed config (`active_ttl: forever`) — bad values silently kept at defaults, init succeeds.
- Missing `--config` path — silently falls back to defaults.

**Versions shipped this session**
- v0.2.0 (B4/B7/init/help)
- v0.2.1 (DX1-DX5/DX8 fixes)
- v0.3.0 (lock + --ignore)
- v0.3.1 (WAL DSN attempt — didn't take)
- v0.3.2 (WAL via PRAGMA — actually fixed)
- v0.3.3 (rename uses relpath)
- v0.4.0 (reconcile cold-start emits)

**Still open / deferred**
- [ ] B2 (RetentionDays plumbed — done in session 1, kept open in error; now closed)
- [ ] `failed` events still have no retry path (engineering report B10) — design call: do we move them back to `pending` on retry, or surface them in a `--retry-failed` command? Deferred for product input.
- [ ] Config parser doesn't warn on unknown / unparseable keys — low priority but worth a `--config strict` flag eventually.
- [ ] No real fsnotify integration. Documented as v1 tradeoff. Deferred.
- [ ] No `--config strict` mode for malformed configs.
- [ ] PMM still needs to: choose a public repo URL, decide whether to ship binary releases (GoReleaser → tagged GH releases), and confirm telemetry posture (currently: none).

**State of the repo at end of session 2:** All blocker and high-value DX issues found through systematic probing are fixed. The binary survives the README Quickstart verbatim, concurrent CLI usage, high-volume churn, unicode/spaces, rename/deletion via reconcile, and process restart. LICENSE, CHANGELOG, CI workflow, and rewritten README are in place. The project is openable as-is, pending the deferred items above (none of which block a v0.4.0 tag).

---

### 2026-05-20 03:27 UTC — claude session 3 (continued probing & feature work)

**Tasks closed**
- [x] **Failed-event retry path** (engineering report B10, previously deferred). New `Store.RequeueFailedEvents` + `Adapter` extension + new CLI: `sharedwatch events retry`. Prints `requeued N failed event(s) to pending` or `no failed events to retry`. Unit test `TestRequeueFailedEvents`. Dogfooded: forced `UPDATE events SET status='failed'` via sqlite3, then `events retry` flipped them back, `consume` then produced a digest.
- [x] **`digest list --status`** filter (`pending|read|archived`). New `Store.ListDigestsFiltered` adapter method. Empty-result message tailored when filter is set: `no digests with status=archived`.
- [x] **CONTRIBUTING.md OSS rewrite** — generic ground rules, PR checklist, style guidance, MIT license attribution. The old version named specific teammates by name (Mike/John/Sarah); externally that read as in-group code, so genericized.
- [x] **Makefile beef-up** — added `ci`, `smoke`, `run`, `clean` targets with `VERSION ?= dev` overridable for build linking. Smoke target spins up a temp `$XDG_DATA_HOME`, runs init → test emit → consume → digest list, prints success and the temp path.
- [x] **CHANGELOG updated** with the new additions and the reconcile cold-start / WAL / rename-relpath changes that I'd shipped in session 2 but hadn't recorded in the file.

**Dogfooding verifications this round**
- `events retry` with 0 failed events → `no failed events to retry` (idempotent).
- `events retry` with 2 failed events → requeued, status reflects `failed=0 pending=2`, follow-up `consume` produces a digest.
- `digest list --status pending` → only pending digests.
- `digest list --status read` → only read digests (after `digest show` flips one).
- `digest list --status archived` → empty-result message.
- Manual smoke equivalent of `make smoke` run end-to-end: clean.

**Versions shipped this session**
- v0.5.0 (events retry + digest status filter + Makefile + CONTRIBUTING + CHANGELOG)

**Remaining deferred (intentional)**
- [ ] `--config strict` mode that errors on unknown keys / unparseable values. Low priority; current lenient behavior is documented behavior.
- [ ] Real fsnotify integration. Documented as v1 tradeoff in README and OPERATOR_GUIDE. Deferred behind a flag for v2.
- [ ] GoReleaser config for tagged binary releases. Needs PMM call on release process / repo URL / signing.
- [ ] Compact digest summary mode (e.g. `digest show --summary` giving "12 modified across 3 files: a.md, b.md, c.md" instead of one line per event). Cosmetic; deferred until users complain.
- [ ] Update `docs/*.md` to reflect the new feature surface (init, events retry, status filter, WAL). Existing docs still describe the v1 design accurately for what they cover; haven't broken anything, but the new commands aren't documented in `OPERATOR_GUIDE.md`. Should be a follow-up pass.
- [ ] Update the *original* tracker `tasklist-2026-03-18.md` — that's Sarah's running log; not mine to touch.

**State of the repo at end of session 3:** 11 packages, all tests green, gofmt + vet clean. Binary at `.bin/sharedwatch` (v0.5.0-20260520). CLI surface now: `init`, `run`, `status [--json]`, `mode active|passive`, `consume`, `digest list [--status] [--limit] | show <id> | archive <id>`, `reconcile now`, `events retry`, `test emit [relpath]`, `version`, `help`. Global flags: `--config`, `--watch-path`, `--db`, `--data-dir`, `--ignore`, `--log-format`, `--log-level`, `--version`. The project ships externally as-is with zero blockers; the only remaining items are explicit product/PMM judgment calls.

