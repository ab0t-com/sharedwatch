# sharedwatch — tasklist: --data-dir umbrella derivation (SW-AGENT-21)

Created: 2026-05-24 06:25 UTC
Owner: claude (this session)
Branch: `main`
Source-of-truth ticket: [`ticket-data-dir-umbrella-derivation-20260524_062538.md`](./ticket-data-dir-umbrella-derivation-20260524_062538.md)
Target release: `v0.0.8`

End-state: `v0.0.8` shipped; `--data-dir <X>` re-derives `WatchPath` + `DBPath` to `X/watch` + `X/queue.db` when those aren't explicitly set; scenario 21 passes end-to-end via `--data-dir`; one agent gotcha removed.

## Legend / Conventions
(Same as prior tasklists.)

===========================================================

## Section 1 — Code change

- [x] **S1.1.** Freeze a snapshot of `config.Default()` after config-file load + env overlay, before the flag-override pass, so we can detect "still at built-in default" cleanly.
      Touches: `src/cmd/sharedwatch/main.go`.

- [x] **S1.2.** When `--data-dir <X>` is set, derive `WatchPath = X/watch` and `DBPath = X/queue.db` IF the user didn't explicitly set them (via flag, env, or config). Explicit flag still wins for each.
      Touches: same.
      Verify: `XDG_DATA_HOME=A sharedwatch --data-dir B init` prints all three paths under B.

---

## Section 2 — Tests

- [x] **S2.1.** New test in `src/cmd/sharedwatch/` covers: bare data-dir derivation; explicit flag wins; config.yaml value preserved.
      Touches: `src/cmd/sharedwatch/main_test.go` (or `env_glue_test.go`).
      Verify: `go test ./cmd/sharedwatch -run TestDataDirUmbrella -count=1` green.

- [x] **S2.2.** Full test suite green.

---

## Section 3 — Re-dogfood scenario 21

- [x] **S3.1.** Re-run scenario 21 against the v0.0.8-build binary using `--data-dir` (instead of `XDG_DATA_HOME`). Confirm watch_path/db_path correctly under the umbrella.
      Touches: nothing on disk; result recorded in worklog.

---

## Section 4 — Docs (remove the gotcha, bump versions)

- [x] **S4.1.** Remove the `--data-dir` bullet from the "Known gotchas" section in `docs/agent/agent-system-prompt-20260522.md`.
- [x] **S4.2.** Remove the corresponding comment from `Skills/sharedwatch-client/SKILL.md` agent-startup ritual.
- [x] **S4.3.** `src/CHANGELOG.md` `[Unreleased]` gains a one-line "Changed —" entry.
- [x] **S4.4.** `man/sharedwatch.1` `.TH` → v0.0.8; the FILES section mentions the `--data-dir` re-derivation.
- [x] **S4.5.** Embedded `usage()` DEFAULT PATHS block reflects the new behaviour.

---

## Section 5 — Release v0.0.8

- [x] **S5.1.** `./scripts/release.sh --version v0.0.8` → `release/v0.0.8/` → bump LATEST → commit + tag + branch + push.
- [x] **S5.2.** Smoke the public installer; verify v0.0.8 version + umbrella derivation.

---

## Worklog

### 2026-05-24 06:25 UTC — claude (this session)
Filed ticket [`SW-AGENT-21`](./ticket-data-dir-umbrella-derivation-20260524_062538.md) + this tasklist. Claiming start-to-finish. Starting at S1.1.
