# sharedwatch — tasklist: smart hints / next-step suggestions (SW-AGENT-17)

Created: 2026-05-24 04:02 UTC
Owner of this list: claude (this session)
Branch: `main` (small additive feature; no feature branch)
Source-of-truth ticket: [`ticket-smart-hints-profiles-20260524_040256.md`](./ticket-smart-hints-profiles-20260524_040256.md)

End-state when this list is fully `[x]`: `sharedwatch v0.0.4` deployed via the in-repo release flow, every applicable command emits context-driven next-step hints in text + JSON, gated by a modular profile-driven engine in `src/internal/hints/` that can be lifted to a standalone module later.

## Legend

- `[ ]` open, not claimed
- `[~]` claimed / in-progress (note the actor in the worklog when you claim)
- `[x]` done
- `[!]` blocked (with a short note on what's blocking)

## Conventions for this list

- Each task names the file(s) it touches and a one-line **Verify** step.
- Sub-tasks are numbered `Sn.m` so the worklog can reference them precisely.
- The worklog at the bottom is **append-only** — don't edit prior entries; add a new entry per claim and per completion.
- Reference: [`SW-AGENT-17 ticket`](./ticket-smart-hints-profiles-20260524_040256.md) §3 (design) + §4 (acceptance).

===========================================================

## Section 1 — Package skeleton (`internal/hints/`)

- [x] **S1.1.** Create the package directory and `hints.go` with the public types: `Profile`, `Hint`, `HintSet`, `Context`, `Provider`, `RootRef`, plus the engine functions `Register(command, provider)` and `For(command, ctx)`.
      Touches: `src/internal/hints/hints.go` (new).
      Verify: `go build ./internal/hints` succeeds; the file exports exactly the symbols listed in §3.2 of the ticket; no imports from `internal/app`, `internal/db`, or `cmd/`.

- [x] **S1.2.** Add `profiles.go` with the four built-in profiles and a `Profile.Limit() int` helper + a `ResolveProfile(flag, env, isJSON string) Profile` helper that implements the resolution rules in §3.3 of the ticket.
      Touches: `src/internal/hints/profiles.go` (new).
      Verify: `go test ./internal/hints -run TestProfile_Resolve` passes for the cases enumerated in §5 of the ticket.

- [x] **S1.3.** Add `render.go` with `RenderText(w io.Writer, set HintSet)` using `text/tabwriter` for alignment; produces nothing when `set` is empty or `profile == off`.
      Touches: `src/internal/hints/render.go` (new).
      Verify: `go test ./internal/hints -run TestRender_Text` passes the golden-output check.

---

## Section 2 — Per-command providers (`internal/hints/providers.go`)

One file, one `func` per command, each `func(Context) []Hint`. Registered in package `init()`.

- [x] **S2.1.** `providerStatus` — emit `consume_pending` (if pending>0), `drain_failed` (if failed>0), `read_digests` (if unread>0), and one `drill_root` per configured root (capped by profile limit).
      Touches: `src/internal/hints/providers.go` (new).
      Verify: `TestProvider_Status_Pending` + `TestProvider_Status_Empty` + `TestProvider_Status_MultiRoot` all pass.

- [x] **S2.2.** `providerRoots` — one `drill_root_<label>` hint per root in `ctx.Roots`, each suggesting `events list --root <label> --since 1h --format jsonl`.
      Touches: same.
      Verify: `TestProvider_Roots_PerRoot` passes.

- [x] **S2.3.** `providerDigestList` — for the newest 1–2 digests, a `show_digest_<id>` hint.
      Touches: same.
      Verify: `TestProvider_DigestList_NewestN` passes.

- [x] **S2.4.** `providerDigestShow` — single hint `archive_digest_<id>`.
      Touches: same.
      Verify: trivial unit test (`TestProvider_DigestShow`).

- [x] **S2.5.** `providerEventsStats` — top type → narrow query; top actor → narrow query.
      Touches: same.
      Verify: `TestProvider_EventsStats_TopType` passes.

- [x] **S2.6.** `providerEventsListCursor` — when 0 events returned: hint `check_status`; when many: hint `summarise_via_stats`.
      Touches: same.
      Verify: `TestProvider_EventsList_Zero` + `TestProvider_EventsList_Many` pass.

- [x] **S2.7.** `providerInit` — single hint `start_watcher` with the resolved watch_path baked in.
      Touches: same.
      Verify: `TestProvider_Init` passes.

- [x] **S2.8.** `providerVersion` — only emits when `ctx.NewerExists`: hint `apply_update`.
      Touches: same.
      Verify: `TestProvider_Version_NewerExists` + `TestProvider_Version_Current` pass.

- [x] **S2.9.** `providerOverview` — generalises the existing `drill` map: by-type, recent, failed, per-top-actor.
      Touches: same + `src/internal/app/overview.go` (drop the literal `drill` map; consume the hints provider in §3.5).
      Verify: `TestProvider_Overview` passes; `overview --format json` still has actionable next-step commands but under the unified `next` array.

---

## Section 3 — CLI integration (`cmd/sharedwatch/`)

- [x] **S3.1.** Add the `--hints` global flag (string; default empty) on the root flagset in `main.go`, and the `SHAREDWATCH_HINTS` env var lookup.
      Touches: `src/cmd/sharedwatch/main.go`.
      Verify: `sharedwatch --hints off status` parses cleanly; `sharedwatch help` lists `--hints`.

- [x] **S3.2.** Add a tiny helper in `cmd/sharedwatch/hints_glue.go` that bridges app state → `hints.Context` (so handlers don't construct the Context inline).
      Touches: `src/cmd/sharedwatch/hints_glue.go` (new).
      Verify: compiles; no logic; pure mapping.

- [x] **S3.3.** Wire `status` text + JSON paths to call `hints.For("status", buildContext(...))` and emit accordingly.
      Touches: `src/cmd/sharedwatch/main.go` (`handleStatus`).
      Verify: `sharedwatch status` shows `Next:` block when pending>0; `sharedwatch status --json --hints off` has no `next` key.

- [x] **S3.4.** Wire `roots`, `digest list`, `digest show`, `events stats`, `events list` (cursor mode), `init`, `version` analogously.
      Touches: `src/cmd/sharedwatch/main.go`, `roots.go`, `update.go` (for `version` adjacent path).
      Verify: each command emits hints per §2 of this tasklist; `--hints off` suppresses on all.

- [x] **S3.5.** Replace the literal `drill` map in `internal/app/overview.go`'s JSON output with the unified `next` array driven by `hints.For("overview", ...)`. Bump the CHANGELOG with the breaking-shape note.
      Touches: `src/internal/app/overview.go`, `src/internal/app/overview_test.go`, `src/CHANGELOG.md`.
      Verify: `overview --format json | jq .drill` returns null; `overview --format json | jq .next` returns array.

---

## Section 4 — Tests

- [x] **S4.1.** Add `src/internal/hints/hints_test.go` with the ≥8 cases enumerated in ticket §5.
      Touches: `src/internal/hints/hints_test.go` (new).
      Verify: `go test ./internal/hints -count=1` green with ≥8 named tests passing.

- [x] **S4.2.** Update `src/internal/app/overview_test.go` to assert `next` shape (not `drill`).
      Touches: `src/internal/app/overview_test.go`.
      Verify: `go test ./internal/app -run TestOverview -count=1` green.

- [x] **S4.3.** Run the full suite locally: `cd src && go test ./... -count=1` — all 11+ packages green.
      Touches: nothing on disk.
      Verify: 0 failures, 0 skips outside expected.

---

## Section 5 — Docs

- [x] **S5.1.** `src/CHANGELOG.md` `[Unreleased]` block: add an "Added — smart hints / next-step suggestions" entry covering the package, the profiles, the `--hints` flag, and the breaking-shape note for `overview`'s removed `drill` key.
      Touches: `src/CHANGELOG.md`.
      Verify: file diff is a single new block under `[Unreleased]`.

- [x] **S5.2.** `src/README.md`: add `--hints` row to the global flags table; add a short example showing the `Next:` block.
      Touches: `src/README.md`.
      Verify: file diff scoped to the global-flags table + one EXAMPLES entry.

- [x] **S5.3.** Top-level `README.md`: in the "For AI agents" section, mention `sharedwatch --hints agent` as the recommended invocation for agents.
      Touches: `README.md`.
      Verify: single-sentence addition in the awareness paragraph.

- [x] **S5.4.** `man/sharedwatch.1`: bump `.TH` version to v0.0.4; add `--hints` to GLOBAL FLAGS; add `SHAREDWATCH_HINTS` to ENVIRONMENT.
      Touches: `man/sharedwatch.1`.
      Verify: `man --warnings -P cat -l man/sharedwatch.1 | grep -i hints` shows the new entry.

- [x] **S5.5.** `Skills/sharedwatch-client/SKILL.md` + `Skills/sharedwatch-client-future/SKILL.md`: mention `--hints agent` as the recommended profile for agents in the relevant agent-quick-reference blocks.
      Touches: those two files.
      Verify: grep for `--hints agent` returns both files.

---

## Section 6 — Release v0.0.4

- [x] **S6.1.** Rebuild + verify: `bash scripts/rebuild.sh` clean; `sharedwatch --hints agent status --json` end-to-end smoke against an isolated `XDG_DATA_HOME`.
      Touches: nothing committed.
      Verify: smoke prints a populated `next` array.

- [x] **S6.2.** Cut release artifacts: `./scripts/release.sh --version v0.0.4`. Move into `release/v0.0.4/`; update `release/LATEST`; commit; tag; branch; push.
      Touches: `release/v0.0.4/*`, `release/LATEST`, new commit + new tag + new branch.
      Verify: `git ls-remote origin refs/tags/v0.0.4` returns a sha; `curl -fsSL https://raw.githubusercontent.com/ab0t-com/sharedwatch/main/release/LATEST` returns `v0.0.4`.

- [x] **S6.3.** Re-test the public installer one-liner: `curl -fsSL https://raw.githubusercontent.com/ab0t-com/sharedwatch/main/scripts/install.sh | bash`. Confirm `sharedwatch version` reports `v0.0.4` and `sharedwatch --hints agent status --json` includes a `next` array.
      Touches: `~/.local/bin/sharedwatch` (reinstall).
      Verify: both checks pass.

---

## Worklog

(Append-only. Add an entry every time you claim, complete, or hit a blocker.)

### 2026-05-24 04:02 UTC — claude (this session)
Filed ticket [`SW-AGENT-17`](./ticket-smart-hints-profiles-20260524_040256.md) + this tasklist. Claiming start-to-finish (no co-owners; pre-1.0 small feature). Branch: `main` (no feature branch). Starting at S1.1.
